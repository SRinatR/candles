
import React from 'react';

// This layout can be simple or include specific navigation for /info pages if needed in the future.
export default function InfoLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
    